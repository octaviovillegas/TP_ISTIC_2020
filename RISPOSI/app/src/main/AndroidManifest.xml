<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ferretexapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Infraestructure.Prinpal"></activity>
        <activity android:name=".Infraestructure.View.NuevoProducto" />
        <activity android:name=".Infraestructure.View.ProductoActivity" />
        <activity android:name=".Client.Historial.controller.HistorialActivity" />
        <activity android:name=".Client.Historial.model.Historial" />
        <activity android:name=".Client.Historial.view.ListadoHistorial" />
        <activity android:name=".Client.pushnotification.HomeNotification" />
        <activity android:name=".Client.ApiConsulta.view.DetalleClima" />
        <activity android:name=".Client.ApiConsulta.view.ListadoClima" />
        <activity android:name=".Owner.BDProductos.ViewModelOwner" />
        <activity android:name=".Owner.View.OwnerHome" />
        <activity android:name=".Client.View.ProductoList" />
        <activity android:name=".Client.Controller.ProductoActivity" />
        <activity android:name=".TiempoEspera" />
        <activity android:name=".Client.ClientHome" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".Registro" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <!--
Manera simple de recibir mensajes en primera plana
                <service
                    android:name=".MyFirebaseMessagingService"
                    android:exported="false">
                    <intent-filter>
                        <action android:name="com.google.firebase.MESSAGING_EVENT" />
                    </intent-filter>
                </service>
        -->
        <service
            android:name=".FirebaseService"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
            </intent-filter>
        </service>
        <!--
         Set custom default icon. This is used when no icon is set for incoming notification messages.
             See README(https://goo.gl/l4GJaQ) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_ic_notification" />
        <!--
 Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />
    </application>

</manifest>