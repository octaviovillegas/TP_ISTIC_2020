<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.applicationlujita">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".productImage.ProductActivity"></activity>
        <activity android:name=".productImage.NewProduct" />
        <activity android:name=".productImage.MainActivityDos" />
        <activity android:name=".Operacion" />
        <activity android:name=".ViewActivity" />
        <activity android:name=".ListadoProductos" />
        <activity android:name=".PrecioProducto" />
        <activity android:name=".CodigoQR" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".AutenticacionActivity">

            <!-- android:theme="@style/SplashTheme"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
    </application>

</manifest>